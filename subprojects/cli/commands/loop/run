#!/usr/bin/env bash
set -euo pipefail

args=()
while [ "$#" -gt 0 ]; do
  case "$1" in
    --finalize)
      if [ "${2:-true}" = "false" ]; then args+=(--no-finalize); fi
      shift 2 ;;
    *) args+=("$1"); shift ;;
  esac
done

exec nu "$STRAP_CMD_DIR/main.nu" "${args[@]}"
