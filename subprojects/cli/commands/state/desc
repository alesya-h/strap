Transform or display canonical strap state.

Usage:
  strap state init
  strap state add-user "hello"
  strap state display-last-message

The command reads state from stdin for transforms and writes the next state to stdout.
