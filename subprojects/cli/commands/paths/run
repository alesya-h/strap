#!/usr/bin/env bash
case "${1:-all}" in
  root) printf '%s\n' "$STRAP_ROOT" ;;
  config) printf '%s\n' "$STRAP_CONFIG" ;;
  global) printf '%s\n' "$STRAP_GLOBAL" ;;
  project) printf '%s\n' "$STRAP_PROJECT" ;;
  session) printf '%s\n' "${STRAP_SESSION:-}" ;;
  work) printf '%s\n' "$STRAP_WORK" ;;
  workspace) printf '%s\n' "$STRAP_WORKSPACE" ;;
  all)
    printf 'STRAP_ROOT=%s\n' "$STRAP_ROOT"
    printf 'STRAP_CONFIG=%s\n' "$STRAP_CONFIG"
    printf 'STRAP_GLOBAL=%s\n' "$STRAP_GLOBAL"
    printf 'STRAP_PROJECT=%s\n' "$STRAP_PROJECT"
    printf 'STRAP_SESSION=%s\n' "${STRAP_SESSION:-}"
    printf 'STRAP_WORK=%s\n' "$STRAP_WORK"
    printf 'STRAP_WORKSPACE=%s\n' "$STRAP_WORKSPACE"
    ;;
  *) echo "Usage: strap paths [root|config|global|project|session|work|workspace|all]" >&2; exit 2 ;;
esac
