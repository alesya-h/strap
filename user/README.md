# User Work Templates

`user/template/.strap` shows the intended project-local mutable work layout.

For a real project, copy or initialize this shape at:

```text
my-project/.strap/
```

The harness treats this as `STRAP_WORK`: sessions, project commands, project tools, project porcelain, local zettelkasten data, logs, and cache live here.
