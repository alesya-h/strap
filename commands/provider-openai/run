#!/usr/bin/env nu

use ./run.nu

def main [command?: string, --model: string = "current", --tools: string = "all", --dimensions: string = ""] {
  open --raw /dev/stdin | from json
  | run ($command | default "") --model $model --tools $tools --dimensions $dimensions
  | to json --indent 2
  | print
}
