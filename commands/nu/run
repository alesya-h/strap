#!/usr/bin/env nu

use ./run.nu

def emit [] {
  let value = $in
  if (($value | describe) =~ '^string') { print --no-newline $value } else { $value | to json --indent 2 | print }
}

def main [command?: string, module?: string] { run ($command | default "") ($module | default "") | emit }
