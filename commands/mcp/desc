Run bundled stdio MCP servers.

Usage:
  strap mcp fs
  strap mcp process
  strap mcp web
  strap mcp agent
  strap mcp all

`all` exposes local Strap tool groups but excludes `jsmcp`; request `jsmcp` explicitly if needed.
