#!/usr/bin/env nu

use ./run.nu

def main [command?: string = "list", --group: string = "all", --json, --internal] {
  run ($command | default "list") --group $group --json=$json --internal=$internal | to json --indent 2 | print
}
