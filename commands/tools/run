#!/usr/bin/env nu

use ./run.nu
alias impl = run

def main [command?: string = "list", --group: string = "all", --json, --internal] {
  impl ($command | default "list") --group $group --json=$json --internal=$internal | to json --indent 2 | print
}
