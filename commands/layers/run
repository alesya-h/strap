#!/usr/bin/env nu
use ./run.nu

def main [command: string = "list", kind?: string] {
  if ($kind | is-empty) { run $command | to json --indent 2 | print } else { run $command $kind | to json --indent 2 | print }
}
