#!/usr/bin/env nu
use ./run.nu
alias impl = run

def main [command: string = "list", kind?: string] {
  if ($kind | is-empty) { impl $command | to json --indent 2 | print } else { impl $command $kind | to json --indent 2 | print }
}
