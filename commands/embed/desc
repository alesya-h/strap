Embed text through the selected provider.

Usage:
  printf '{"texts":["hello"]}' | strap embed --provider openai
  printf '{"texts":["hello"]}' | strap embed --provider openrouter

Embedding requests are delegated through `strap provider <name> embed` when that provider supports embeddings. OpenRouter is the default provider.
