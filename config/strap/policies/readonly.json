{
  "name": "readonly",
  "description": "Read-only policy.",
  "rules": {
    "execute": "sandbox",
    "commands": {
      "allow": ["commands", "help", "paths", "policy", "session", "state", "state-bb", "zk"]
    },
    "read_roots": ["root", "config", "project", "work", "workspace"],
    "write_roots": ["work"]
  }
}
