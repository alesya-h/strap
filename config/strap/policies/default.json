{
  "name": "default",
  "description": "Default permissive development policy.",
  "rules": {
    "execute": "allow",
    "commands": { "allow": ["*"] },
    "read_roots": ["*"],
    "write_roots": ["project", "work", "workspace"]
  }
}
